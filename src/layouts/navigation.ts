import { ListType } from "definition";

const pages = {
  menu: [
    {
      title: "Create channel",
      type: ListType.Create,
    },
    {
      title: "Message list",
      type: ListType.Message,
    },
  ],
};

export default pages;
